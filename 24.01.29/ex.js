            // let number1 = 123;
	        // let number2 = 453;
	        // let number3 = 783;

	        // // 세 숫자의 마지막 숫자가 같은지 확인하는 함수 호출
	        // if lastDigitsNum(number1, number2, number3) {
	        //    console.log("세 숫자의 마지막 숫자가 같습니다.");
	        // } else {
            //     console.log("세 숫자의 마지막 숫자가 다릅니다.");
	        // }

	        // // 세 숫자의 마지막 숫자가 같은지 확인하는 함수
	        // // 각 숫자의 마지막 숫자를 추출
	        // let lastDigit1 = num1 % 10;
	        // let lastDigit2 = num2 % 10;
	        // let lastDigit3 = num3 % 10;

	        // // 마지막 숫자가 같은지 확인
	        // return (lastDigit1 == lastDigit2) && (lastDigit2 == lastDigit3);
	        
	        
	        
            // function transformString(input) {
            //     // 문자열의 길이가 3보다 작은 경우
            //     if (input.length < 3) {
            //         // 모두 대문자로 변환
            //         return input.toUpperCase();
            //     } else {
            //         // 처음 세 글자를 소문자로 변환하고 나머지 부분은 그대로 유지
            //         return input.substring(0, 3).toLowerCase() + input.substring(3);
            //     }
            // }
            // // 예제 문자열
            // var inputString = "AbDDDDDDCdef";
            
            // // 변환 함수 호출
            // var result = transformString(inputString);
            
            // // 결과 출력
            // console.log("변환 결과: " + result);

           
          
    
        
            

            