24.01.15
css
transform
translate(x, y, z(축)) -> translateX( ) ->이동
rotate(x, y, z) -> rotateX( ) ->회전
scale(x, y) -> scaleX( ) ->크기
skew(x, y) -> skewX( ) ->각도
matrix(크기x, 각도y, 각도x, 크기y, 이동x, 이동y);

transition
transition-delay 언제부터 시작 할 것인가?
transition-duration 어느 시간동안 진행 할 것인가?
transition-property 무엇을 애니메이션 할 것인가?
transition-timing-function

animation-name: 에니메이션의 이름을 지정한다 @keyframes를이용
animation-duration: 애니메이션되는 시간을 설정한다
animation-delay: 애니메이션이 시작되는 시간을 설정한다.
animation-timing-function: 애니메이션 전환 함수를 이용하여 애니메이션 설정
animation-direction: 애니메이션이 한번 종료되고 처음부터 다시 시작 or 역방향으로 시작할지 결정
animation-interation-count: 애니메이션이 몇번 반복될지 결정 infinite를 이용
animation-fill-mode: 애님이션이 시작전이나 끝나고 난 후 어떤 값이 적용될지 

