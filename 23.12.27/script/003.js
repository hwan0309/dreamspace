/*
A = B; //B를 A에 대입한다
A == B; // A와 B는 같다 (100 == "100") 타입이 달라도됨
A === B; // A와 B는 같다 (100 === 100) 타입까지 같아야됨
A += B; //A = A + B
A *= B; //A= A * B;
A /+ B; //A = A / B
A %= B; //A = A % B
*/

let num1 = 20;
let num2 = 10;
let result = num1 %= num2;
console.log(result);  